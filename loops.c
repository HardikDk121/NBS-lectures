#include <stdio.h>

int main()
{
    /*
    for (int i = 0; i < 10; ++i)
    {
        printf("%d",i);
        break;
    }
    for (int i = 0; i < 10; i++)
    {
            if (i==3)
            continue;

        printf("%d",i);
        
    }
    for (int i =0; ++i <= 5; i++)
    {
       
        printf("\n%d",i);
    }
    */
    /*
    while (flag)
    {
        printf("Enter 0 for exit or enter a number to add :");
        scanf("%d",&flag);
        sum = sum+ flag;
    }
    printf("Sum is %d",sum);
    */
         
    return 0;
}